type User {
  id: ID! @id
  user_name: String!
  email: String!
  post: [Post!]
}
type Post {
  id: ID! @id
  title: String
  owner: User!
  skill: [Skill!]
}

type Skill {
  id: ID! @id
  name: String!
  post: [Post!]
}
